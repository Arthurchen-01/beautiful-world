# Changelog

本项目所有值得记录的变更都写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.1] - 2026-09-24

> 版本跨度：**1.1.0 → 1.1.1**
> 基线提交：`1a36473`（tag `v1.1.0`）→ 本次
> 代码量：`git diff --numstat v1.1.0 HEAD`（**不含本文件自身**）→ **9 个文件，`+395 / −11`**
> 性质：**纠正一个传错了的结论 + 补产能实测 + 出一页版说明书**

### 起因

客户方（韦德）看了说明书后问了三个问题：

> · 「这个不能只测 win11 系统，我们都是用服务器来跑」
> · 「我感觉你这说明书看着都复杂」
> · 「**一次别跑太多，并行数不要超过 8 是什么意思**」
> · 「我们都是几十万几百万数据的跑，**十分钟都要跑十万个**」

第三个问题逼出了一次复查 —— **「并行不超过 8」这个结论是错的**，而且它已经
从 `docs/PROXY_POOL.md` 一路抄进了 `使用说明.txt`、`交付说明.md`、`README.md`
和程序自己的 `--help` / GUI 提示语。**客户就是照着说明书读到的这句。**

### 上一版（1.1.0）的状态 —— 改之前是什么样

| 位置 | 1.1.0 时的原文 | 问题 |
| :--- | :--- | :--- |
| `docs/使用说明.txt` | 「一次别跑太多，**并行数不要超过 8**（实测超过会被风控）」 | 客户直接读到的就是这句 |
| `docs/交付说明.md` | 「有代理池可开 4~8，没代理池最多 2」 | 同上 |
| `README.md` | 「单 IP 最多 2；有代理池可到 8，**超过 8 会被风控**」 | 同上 |
| `src/wm_scan.py` | `--parallel` 帮助：「实测 2 最优；3 会触发风控失败」 | 程序内也在传这个错 |
| `src/wm_gui.py` | GUI 提示：「有代理池可开 8~16」 | 同上 |

**这个「8」从来没有实测依据。** 溯源到 `docs/PROXY_POOL.md` 里那次
「4 账号并行失败」—— 那次的真实根因是**同一个账号开了 4 个并发会话**，
被 Vanguard 按账号踢掉。**是测试设计错了，被误读成并发上限。**

### 本次改动的文件 —— 改了什么

| 文件 | 类型 | 改动行数 | 改了什么 |
| :--- | :--- | ---: | :--- |
| `docs/产能分析_能跑多快卡在哪.md` | **新** | +201 | 实测吞吐数据、瓶颈定位（第一跳饱和在 1.5 req/s）、三档扩产方案、可直接发给客户的话术 |
| `docs/使用说明_一页版.md` | **新** | +130 | 一页版说明书：三步上手 + 界面数字怎么看 + 常见问题 + 命令行参数 |
| `README.md` | 改 | +27 / −5 | 已知限制表重写；新增「⚠️ 更正」区块；文档索引分「给使用者 / 给运维」两组 |
| `docs/PROXY_POOL.md` | 改 | +19 / −1 | 在原始那条错误结论下面加**更正块**，说明「8」是怎么来的、真实规则是什么 |
| `docs/使用说明.txt` | 改 | +7 / −1 | 把「不要超过 8」换成正确规则 + 超时建议 |
| `src/wm_scan.py` | 改 | +5 / −1 | `--parallel` 帮助文本改正 |
| `src/wm_gui.py` | 改 | +4 / −1 | GUI 并行输入框提示语改正 |
| `docs/交付说明.md` | 改 | +1 / −1 | 参数表那一行改正 |
| `VERSION` | 改 | +1 / −1 | `1.1.0` → `1.1.1` |
| `CHANGELOG.md` | 改 | 本节 | — |

### Fixed

| 症状 | 根因 |
| :--- | :--- |
| 客户问「并行不要超过 8 是什么意思」 | 那个数字是**误读**：真实根因是「同一个账号开多个并发会话」，不是并发上限 |
| 说明书被评价「看着都复杂」 | 原来只有一份面向开发者的长文档；现在拆出一页版 |

### Added

- **产能实测**：起 48 个本地桥接端口（各走一条不同的住宅出口）同时压目标站，量吞吐

  | 并发 N | 墙钟 | 成功 | 吞吐 |
  | ---: | ---: | ---: | ---: |
  | 1 | 6.3 s | 1/1 | 0.16 req/s |
  | 4 | 5.4 s | 4/4 | 0.74 req/s |
  | 16 | 12.1 s | 15/16 | 1.33 req/s |
  | 32 | 20.4 s | 29/32 | 1.57 req/s |
  | 48 | 31.4 s | 43/48 | **1.53 req/s** ← 饱和 |

  **并发 32 → 48 吞吐不再增长 ⇒ 瓶颈在第一跳，不在工具、不在代理池。**

- **端到端账号吞吐**（假号，走「登录失败」这条最快的路径）：

  | 并行 | 账号数 | 墙钟 | 吞吐 |
  | ---: | ---: | ---: | ---: |
  | 4 | 4 | 18 s | 800 账号/时 |
  | 16 | 16 | 54 s | 1,067 账号/时 |

  > 4 → 16 并发只带来 1.33 倍吞吐，与上面的「第一跳饱和」一致。

### Verified

**「同一个账号不能并发」这条规则是真的**，但「不同账号可以高并发」也验证了：

```
[扫描] 16 个账号同时跑，每个账号内部限速 0.8s/请求
[1/16] ... [16/16] 每个账号分到独立的日本住宅出口 IP
本轮 完成 16/16  用时 0.9 分
===== 完成：成功 0 / 失败 16 / 跳过 0 =====
```

**16 个互不相同的账号在 `--parallel 16` 下没有任何风控报错** —— 直接证伪了「8 是上限」。

**新 exe**：84.0 MB，`--selftest` 全部通过（含真实 OCR 实拍），已替换到桌面。

### Known Issues

| 问题 | 状态 |
| :--- | :--- |
| **产能瓶颈**：第一跳饱和在 1.5 req/s | ⛔ 距「10 万/10 分钟」需要的 1,400 req/s 差约 930 倍。**解法是部署到香港服务器**，把第一跳换成数据中心线路 —— 但**必须在服务器上实测才算数** |
| 服务器 `154.219.105.203` 不可达 | ⛔ 机器本身没起来（15 个全球节点 + 本机物理直连全部超时） |
| Windows Server 未真机验证 | ⚠️ `deploy_server.ps1` 已就绪但只在 Win11 上 DryRun 过；客户要求「不能只测 Win11」 |
| 「无感通过」命中率 | ⚠️ 样本不足 |

## [1.1.0] - 2026-09-24

> 版本跨度：**1.0.2 → 1.1.0**
> 基线提交：`2a63f84`（tag `v1.0.2`）→ 本次
> 代码量：`git diff --numstat v1.0.2 HEAD`（**不含本文件自身**）→ **10 个文件，`+763 / −24`**
> 性质：**新增服务器部署工具链 + 修一批"看起来是绿的、其实没测/报反了"的问题**

### 上一版（1.0.2）的状态 —— 改之前是什么样

| 项目 | 1.0.2 时的状态 |
| :--- | :--- |
| 服务器部署 | **没有任何部署工具**。要手工建目录、拷文件、写批处理、开任务计划器 |
| HTTP 超时 | `wm_scan.py` / `solve_pick_text.py` 里**写死 30 / 40 / 25 秒**，没法调 |
| 走住宅代理的后果 | 验证码图片 CDN（腾讯 COS）经日本住宅代理要 **5~14 秒**一张，并发一高就 `ReadTimeout`，账号被判 `INCOMPLETE` 反复重跑 |
| `build_exe.py` 的 `OCR_SRC` | 是脱敏占位符 `<PROJECT_DIR>\识别端`。**直接跑会打出一个没有 OCR 的残废包**，而且只在日志里打一行"跳过"就过去了 |
| `build_exe.py` 的成败判定 | 只看**文件是否存在**。PyInstaller 报错后留下一个 **0.3 MB 的空壳**，脚本照样打印 `OK 产物` |
| `build_exe.py` 的图标 | 直接引用 `src/app.ico`，但那文件不入库 → `FileNotFoundError` |
| 自检的样本图 | 只找 `src/samples`，而样本图在**仓库根目录** → 打进包里的样本为空，自检第 [4] 项**静默跳过** |
| `egress_guard` 隧道自查 | 默认目标是 `us.proxy.example` —— **脱敏占位符，根本解析不了**。解析失败时 `in_tunnel=None`，被显示成 **`!! 裸奔`**（把"没连上"报成"泄漏"，恰好是反的） |

### 本次改动的文件 —— 改了什么

| 文件 | 类型 | 改动行数 | 改了什么 |
| :--- | :--- | ---: | :--- |
| `tools/deploy_server.ps1` | **新** | +449 | Windows 服务器一键部署：六段体检（组件/系统/输入/铺文件/计划任务/冒烟）+ `-DryRun` + `-SkipSmoke` + `-Uninstall` |
| `src/build_exe.py` | 改 | +106 / −13 | 识别端位置改为「环境变量 → 常见位置兜底 → 明确报错」；缺 `app.ico` 自动生成；样本图补上仓库根目录；**成败按退出码 + 产物大小双重判定**（10 MB 门槛，堵住"0.3 MB 空壳报 OK"） |
| `docs/网络链路修复_20260924.md` | 改 | +64 | 新增「走住宅代理链的新瓶颈：验证码图片 CDN 太慢」整节（含实测耗时表与修法） |
| `src/egress_guard.py` | 改 | +52 / −1 | 隧道自查的默认目标改为**从代理文件里取真实主机**（去重、最多 3 个）；没有代理文件时明确返回 `skipped` 而不是拿占位符假装查过 |
| `src/wm_scan.py` | 改 | +35 / −4 | `HTTP_TIMEOUT` / `POST_TIMEOUT` 改为可配（`--timeout` / `WM_HTTP_TIMEOUT` / `WM_POST_TIMEOUT`），默认值不变；三处写死的 30/40/25 全部改用常量；启动时打印 `[超时] HTTP=xxs POST=xxs` |
| `src/wm_gui.py` | 改 | +14 / −2 | 自检第 [2] 项改三态显示：`连不上` 不再报成 `裸奔`；跳过时给出明确说明 |
| `src/solve_pick_text.py` | 改 | +11 / −2 | 超时同样从 `WM_HTTP_TIMEOUT` 读 |
| `.gitignore` | 改 | +4 | 图标是 `make_icon.py` 的生成物，不入库（可逆：需要时跑一次就回来了） |
| `README.md` | 改 | +27 / −1 | 版本号 → v1.1.0；目录树补 `deploy_server.ps1`；新增「部署到 Windows 服务器」整节；`--timeout` 参数说明 |
| `VERSION` | 改 | +1 / −1 | `1.0.2` → `1.1.0` |
| `CHANGELOG.md` | 改 | 本节 | — |

### Added

- **`tools/deploy_server.ps1`** —— 服务器一键部署，六段体检：
  1. **组件体检**：Media Foundation（`MF.dll` / `MFPlat.DLL` / `MFReadWrite.dll`）、
     Direct3D 11（`d3d11.dll`）、VC++ 运行库、**32 位 `vcomp140.dll`**（这个缺了 OCR 会崩 `0xC000041D`）
  2. **系统体检**：位数、**是不是 Server Core**（没有 `explorer.exe` 就是）、内存、磁盘、端口 506
  3. **输入检查**：exe / 账号文件 / 代理文件格式
  4. **铺文件**：建目录、拷 exe、生成 `run_scan.cmd`（带日志轮转，只留最近 30 份）
  5. **计划任务**：开机自启 + 每 N 分钟一次；**非管理员时明确跳过并给出 `schtasks` 兜底命令**
  6. **冒烟测试**：跑一次 `--selftest`
- `--timeout N`：住宅代理链建议 60

### Fixed

| 症状 | 根因 |
| :--- | :--- |
| 打包失败但脚本报 `OK 产物`，产物只有 0.3 MB | 只检查文件存在，不检查大小。改成退出码 + 大小（>10 MB）双重判定 |
| 打包 `FileNotFoundError: app.ico` | `app.ico` 是生成物不入库。改成缺了就自动跑 `make_icon.py` |
| 打出来的包**没有 OCR**，只在日志里一行"跳过" | `OCR_SRC` 是脱敏占位符。改成环境变量 + 兜底探测 + **缺文件直接 `exit 2` 拒绝出包** |
| 自检第 [4] 项 OCR 实拍静默跳过 | 样本图只找 `src/samples`，实际在仓库根目录 |
| 自检打印 `!! 裸奔`，看着像泄漏 | 默认目标 `us.proxy.example` 是占位符，解析失败被当成"裸奔"。改成三态 + 从代理文件取真实主机 |
| 需要解验证码的账号反复 `INCOMPLETE` | 验证码图片 CDN 经住宅代理要 5~14 秒，撞上写死的 30 秒超时 |
| `deploy_server.ps1` 在 PowerShell 5.1 下**解析失败** | 脚本是 UTF-8 无 BOM，PS 5.1 按 GBK 解码中文全乱。改成 **UTF-8 with BOM** |
| `deploy_server.ps1` 卡死不返回 | 用 `dxdiag /t` 探 WARP，而 dxdiag 是 GUI 程序会挂住。改成 `Win32_VideoController` 查询 |

### Verified

**全量代理池 + 端到端真账号扫描**（详见 1.0.2 条目，本版沿用）：

```
可用 94/101（复核 101/101）   唯一出口 IP 87   国内出口 0
[1/1] nomanum@163.com 出口 121.83.4.28 (JP/Higashiosaka)
扫描完成 53/53 区服，发现角色 2，服务器 2，无感通过校验
结果 nomanum@163.com----sheep1027----[校园区-天戎----120----1----[校园区-御龙----1]
```

**新 exe 打包与自检**：

```
[build] 打包样本图: D:\dev_tools\beautiful-world\samples  (5 张)
[build] 打包识别端: D:\验证码\识别端  (80.2 MB)
[build] OK 产物: src\dist\完美世界扫号工具.exe
[build]    大小: 84.0 MB
[build]    校验: 含识别端

--- exe --selftest ---
[1] 识别端    vcomp140: True   服务已在监听 506
[2] 出口红线  （没有代理文件，隧道绑定自查跳过 —— 不是出错）  OK 出口 103.151.172.36 (Hong Kong)
[3] 依赖      requests / Crypto / socks / tkinter 全 OK
[4] 本地 OCR 实拍   cap_00.jpg -> 113,108|249,46|240,119|48,252|187,38|
=== 结论: 全部通过 ===
```

**超时修复在 exe 里真的生效**（这条只能靠跑，字符串搜索会被 pyc 压缩骗到）：

```
set WM_HTTP_TIMEOUT=60 && 完美世界扫号工具.exe --cli ...
→ scan.log: [超时] HTTP=60s  POST=70s
```

**`deploy_server.ps1` 本地验证**：

- `-DryRun`：通过 16 项 / 警告 5 项 / 失败 0 项，正确识别出「32 位 vcomp140.dll 不在系统目录」
- 真安装到临时目录：exe / 账号.txt / 代理.txt / `run_scan.cmd` / `手动跑一次.cmd` 全部就位；
  **非管理员时正确跳过计划任务**并打印 `schtasks /create ...` 兜底命令

### Known Issues

| 问题 | 状态 |
| :--- | :--- |
| 服务器 `154.219.105.203` 不可达 | ⛔ 机器本身没起来（15 个全球节点 + 本机物理直连全部超时） |
| `deploy_server.ps1` 未在真服务器上跑过 | ⚠️ 只在本机 DryRun + 临时目录验证过；服务器起来后需实跑一次 |
| 同一个 sid 的出口 IP 会轮换 | ⚠️ 见 1.0.2；「一账号一 IP」不能只靠 sid 保证 |
| 「无感通过」命中率 | ⚠️ 样本不足（2 个账号里 1 个命中） |
| 桥接器 / 隧道长期稳定性 | ⚠️ 未做长跑测试 |

## [1.0.2] - 2026-09-24

> 版本跨度：**1.0.1 → 1.0.2**
> 基线提交：`0cab50d`（tag `v1.0.1`）→ 本次
> 代码量：`git diff --numstat v1.0.1 HEAD`（**不含本文件自身**）→ **4 个文件，`+211 / −24`**；`src/` **0 改动**
> 性质：**缺陷修复 + 全量验证** —— 修掉一个会把好代理误判成坏代理的探针 bug

### 上一版（1.0.1）的状态 —— 改之前是什么样

| 项目 | 1.0.1 时的状态 |
| :--- | :--- |
| `chain_proxy.probe()` | 直接 `json.loads(ipinfo.io/json)` 取出口 IP。**101 条并发时被限流，返回非 JSON**，`JSONDecodeError` 被当成"代理不可用" |
| 体检假阴性 | 首次全量体检报 **7 条不可用** —— 实测**这 7 条全是好的**，是探针的锅 |
| 后台运行 | 桥接器只往 stdout 打日志。用 `Start-Process` 起的子进程挂在父进程 job object 上，**父进程一被强杀，子进程和日志一起没** |
| 池容量 | 未测过全量，只知道 8 条可用 |
| 出口 IP 稳定性 | 未验证过 |
| 端到端 | 只验到「代理能通目标站」，**没跑过真账号** |

### 本次改动的文件 —— 改了什么

| 文件 | 类型 | 改动行数 | 改了什么 |
| :--- | :--- | ---: | :--- |
| `tools/chain_proxy.py` | 改 | +88 / −22 | 探针重写（ipify 纯文本 IP + 归属地分离 + 重试 3 次）；新增 `--log` 自写日志；新增 `--check-only` 一次性体检模式 |
| `docs/网络链路修复_20260924.md` | 改 | +121 | 补入全量体检结果、探针假阴性复盘、出口 IP 轮换发现、**端到端真账号验证** |
| `README.md` | 改 | +1 / −1 | 版本号 → v1.0.2 |
| `VERSION` | 改 | +1 / −1 | `1.0.1` → `1.0.2` |
| `CHANGELOG.md` | 改 | 本节 | — |
| `src/` 全部 | — | **0** | 业务逻辑一行没动 |

### Fixed

| 症状 | 根因 |
| :--- | :--- |
| 7 条好代理被判"不可用" | 探针拿 `ipinfo.io/json` 当唯一来源，**并发一高就被限流返回非 JSON**。改成先取 `api.ipify.org` 的纯文本 IP（不会被 JSON 解析坑），再补归属地，并重试 3 次 |
| 后台跑的桥接器日志丢失 | `Start-Process` 的子进程挂在父进程 job object 上，父进程被强杀时子进程一起死。新增 `--log` 让桥接器自己落盘，配合 WMI `Win32_Process.Create` 真正脱离进程树 |

### Added

- `--check-only`：跑完体检、写出可用清单就退出，不常驻（适合定期刷新池）
- `--log FILE`：日志同时写文件，后台运行可事后查

### Verified

**全量体检（101 条 sid）**：

```
可用 94/101（探针修复后复核 → 实际 101/101）
唯一出口 IP 87    国内出口 0
出口 ISP: NTT DOCOMO / KDDI / SoftBank / Sony / Asahi / OPTAGE / ARTERIA / BIGLOBE / Rakuten / NTT PC / GLBB
```

**★ 端到端真账号扫描（这是链路可用的最终证据）**：

```
[egress] 出口 IP = 103.151.172.36  (Hong Kong / Mong Kok)
[池体检] 可用 92/101   唯一出口 IP 76   国内出口 0
[红线]   OK 全部出口不在国内，可开工 1
[1/1] nomanum@163.com  出口 121.83.4.28 (JP/Higashiosaka, 2389ms)
   [识别] 游戏: ['完美世界', '笑傲江湖OL']  -> gameId [1, 23]
   扫描完成 53/53 区服，发现角色 2，服务器 2
   无感通过校验，无需自动打码 ✓
```

**结果**（与 09-23 那次逐项一致）：

```
nomanum@163.com----sheep1027----[校园区-天戎----120----1----[校园区-御龙----1]
```

**出口 IP 轮换实测**（间隔约 2 分钟）：

```
ApJFvYFH   133.206.160.96  -> 133.206.160.96   稳定
Y2Vwf1Lz   59.132.67.253   -> 59.132.67.253    稳定
JFd75SCb   124.33.156.242  -> 121.81.235.185   变了
vzMEjX89   126.159.73.185  -> 126.159.73.185   稳定
```

### Known Issues

| 问题 | 状态 |
| :--- | :--- |
| **同一个 sid 的出口 IP 会轮换** | ⚠️ `sid` 是粘性会话令牌，不是固定 IP。换的仍是日本住宅 IP，不暴露本机；但「账号之间绝不共用 IP」这条不变量**不能只靠 sid 保证**。建议开工前实测并记录每个账号的出口 IP |
| 代理池延迟 2.6s ~ 28s | ⚠️ 慢的可能与并发排队有关（iKuuu 第一跳可能串行化），未深查 |
| 桥接器长期稳定性 | ⚠️ 仍未做长跑测试 |
| 「无感通过」命中率 | ⚠️ 只跑 1 个账号，样本不足。**若命中率高，点选验证码的 CV 问题会大幅降级** |
| 服务器 `154.219.105.203` 不可达 | ⛔ 机器本身没起来（15 个全球节点 + 本机物理直连全部超时） |

## [1.0.1] - 2026-09-24

> 版本跨度：**1.0.0 → 1.0.1**
> 基线提交：`8759c1f`（tag `v1.0.0`）→ `9e6a7af`
> 代码量：`git diff v1.0.0 HEAD --numstat`（**不含本文件自身**）→ **7 个文件，`+1277 / −2`**；`src/` **0 改动**
> 性质：**网络链路修复** —— 不改业务逻辑，只解决"跑不起来 / 连不上"

### 上一版（1.0.0）的状态 —— 改之前是什么样

| 项目 | 1.0.0 时的状态 |
| :--- | :--- |
| 代理池 | 代码写的是 `socks5h://user:pass@host:port` **直连**。实测**从大陆一律被拒**：`403 forbidden ip=<本机公网IP> not supported` |
| 出口隔离 | `egress_guard.py` 能体检，但**没有任何办法让代理池真的能用** |
| 非 80/443 端口 | 本机默认路由走 iKuuuVPN 的 TUN（fake-ip），**对所有 TCP 假握手后黑洞**。RDP / WinRM / SMB 全部连不上，且端口扫描结果全是假的 |
| 服务器部署 | 上不去（服务器本身没起来，且即使起来也连不上 3389） |
| 服务器状态判断 | 曾据"端口全开"判断"机房防火墙在应答" —— **这个判断是错的** |

### 本次改动的文件 —— 改了什么

`git diff --numstat v1.0.0 HEAD`（逐文件行数，直接抄）：

| 文件 | 类型 | 改动行数 | 改了什么 |
| :--- | :--- | ---: | :--- |
| `tools/chain_proxy.py` | **新** | +411 | 代理池第一跳桥接器：把「本机→香港→日本住宅」封装成普通本地 SOCKS5，每条 sid 一个端口。**`proxy_pool.py` 零改动** |
| `docs/网络链路修复_20260924.md` | **新** | +320 | 完整排查报告：TUN 假握手、代理拒大陆 IP、服务器状态，全部带对照实验 |
| `tools/tunnel.py` | **新** | +296 | 本地端口转发（RDP/WinRM/SMB/SSH），带**权威探活** |
| `tools/server_ready.py` | **新** | +217 | 服务器就绪监视器，只认真协议回复，可 `--watch` 轮询 |
| `README.md` | 改 | +30 / −1 | 版本号 → v1.0.1；目录树补 3 个新工具；新增「代理池必须先过第一跳」小节；文档索引 |
| `.gitignore` | 改 | +2 | 放行 `tools/server_ready.py`（第 28 行 `server_*.py` 本意是排临时诊断脚本，误伤了正式工具） |
| `VERSION` | 改 | +1 / −1 | `1.0.0` → `1.0.1` |
| `CHANGELOG.md` | 改 | 本节 | — |
| `src/` 全部 | — | **0** | 业务逻辑一行没动 |

### Added

- **代理池可用性修复**：新增第一跳桥接，解决 arxlabs 拒绝大陆来源 IP 的问题
- **权威探活**：`probe_target()` 只认「真协议回复」——
  443 真 TLS 握手、3389 发 RDP TPKT、445 发 SMB negotiate、22 等服务端报 banner。
  **不信 HTTP 代理对 CONNECT 的乐观 `200`**
- **端口转发**：`tools/tunnel.py --map 13389:<host>:3389` → `mstsc /v:127.0.0.1:13389`
- **服务器就绪监视**：`tools/server_ready.py --watch 30`

### Fixed

| 症状 | 根因 |
| :--- | :--- |
| **日志整行凭空消失** | 控制台是 GBK，日志里的 `✗` `★` `→` 让 `print` 抛 `UnicodeEncodeError`，而 `say()` 的 `except: pass` **把它吞了**。两个新工具已加 `stdout.reconfigure(encoding="utf-8", errors="replace")` |
| 探活把活的服务报成"不通" | 手搓的 TLS ClientHello 是残缺的，服务端不会回话。改成用 `ssl.wrap_socket()` 做**真握手** |
| 端口扫描全"开放" | TUN 的 fake-ip 对任何 TCP 本地假握手。**拿测试保留地址 `203.0.113.7` 去连也是"全端口开放"** |

### Verified

**代理池链路（8 条 sid，全部可用）**：

```
体检结果：可用 8/8   唯一出口 IP 8   国内出口 0

 20001  ApJFvYFH   106.146.215.4   Osaka/KDDI         4003ms
 20002  QqUXUeT1   59.132.67.253   Tokyo/KDDI         2731ms
 20003  JFd75SCb   106.155.11.40   Tokyo/KDDI        28584ms
 20004  aVNVHqtB   60.141.5.68     Kitakyushu/SoftBank 27791ms
 20005  RPjaxjzJ   153.252.81.130  Higashiosaka/NTT   27946ms
 20006  hep9Jw2h   121.87.209.13   Osaka/OPTAGE       28892ms
 20007  QYmizM1J   60.152.178.240  Suita/SoftBank      2637ms
 20008  MtRs6swi   118.110.47.28   Osaka/BIGLOBE      28239ms
```

**`requests` 兼容性（证明能当普通 `socks5h://` 用）**：

```
socks5h://127.0.0.1:20001 → 106.146.215.4   2982ms
socks5h://127.0.0.1:20007 → 60.152.178.240  2637ms
```

**目标站端到端**：

```
GET passport.wanmei.com/sso/servlet/ajax?op=mCaptchaInit&isAICap=1
  20001  HTTP 200  7265ms  {"data":{"appId":"10003","capTicket":"c59a264c..."},"code":0}
  20007  HTTP 200  3639ms  {"data":{"appId":"10003","capTicket":"988d7029..."},"code":0}
```

**权威探活正确性**：

```
[不通]  154.219.105.203:3389   -> [无任何回应]              ← 真话（服务器确实没起来）
[通]    passport.wanmei.com:443 -> [TLS 握手成功] TLSv1.2   ← 真话
[通]    198.44.166.3:3010      -> [有真回应] 0500           ← SOCKS5 问候应答
[通]    github.com:443         -> [TLS 握手成功] TLSv1.3
```

**exe 自包含性核对**：`完美世界扫号工具.exe`（81.7 MB）内已含
`vcomp140.dll` / `OCR.dll` / `XYLib.dll` / `HPSocket4C.dll` / `Config.ini`，
**服务器上不需要额外装 VC++ 运行库**。

### Known Issues

| 问题 | 状态 |
| :--- | :--- |
| **服务器 `154.219.105.203` 不可达** | ⛔ **机器本身没起来**（15 个全球节点 + 本机物理直连全部超时）。需在商家控制台 Web VNC 确认实例是否已开机 |
| 代理池延迟 2.6s ~ 28s | ⚠️ 慢 sid 可能是冷启动，未做长时间观察 |
| 桥接器长期稳定性 | ⚠️ 未做长跑测试 |
| 50 条全量 sid 可用率 | ⚠️ 本次只验了 8 条 |
| Windows 10 / 8.1 / 7 未真机验证 | 代码有完整降级链，但没真机跑过 |

## [1.0.0] - 2026-09-24

> 版本跨度：**首次发布**（本仓库此前为空）
> 代码量：**22 个文件**（不含 samples 与模板）
> 基线：无（新建仓库）

### 上一版的状态 —— 改之前是什么样

**本仓库此前是空的，没有上一版。** 但为了说明这一版交付了什么，
这里列出「动手之前，这件事是怎么做的」：

| 项目 | 动手前的状态 |
| :--- | :--- |
| 老板的 `角色查询-诛仙.exe` | **已停掉**，没有可用工具 |
| 查角色接口 | 未逆向，不知道 `getRoleListByServerJsonp` 怎么调 |
| 登录链路 | 未打通。`logintype` 传空串会被静默拒绝，缺浏览器头拿不到 JSESSIONID |
| 验证码 | 未解。`WanmeiCaptcha` 的 `pick_text` 点选类型，需要 AES + 指纹 + 行为事件 |
| Vanguard 机器人校验 | 未处理，一触发就死 |
| 区服列表 | 未知，不知道一共多少个区 |
| 本地 OCR | 有一个第三方识别引擎 `图像识别POST服务.exe`，但**要靠手点「启动」按钮**才工作 |
| 代理 | 无池化，单 IP 串行 |
| 结果格式 | 未知（后来才知道要跟老板原软件逐字节一致） |

### 本次交付的内容

| 文件 | 类型 | 作用 |
| :--- | :--- | :--- |
| `src/wm_scan.py` | **新** | 扫描引擎：登录 → 53 区服并发查询 → Vanguard 解锁 → 结果表 |
| `src/wm_gui.py` | **新** | 图形界面：手动输入 / 文件选择 / 环境自检 / 进度 / 日志降噪 |
| `src/wmcaptcha.py` | **新** | 验证码协议逆向实现（AES 加密、指纹生成、拟人行为事件） |
| `src/solve_pick_text.py` | **新** | 点选验证码端到端求解链路 |
| `src/ocr_service.py` | **新** | 本地 OCR 服务管理：自动拉起、点「启动」、探活、隐藏窗口 |
| `src/proxy_pool.py` | **新** | 代理池：体检 / 轮换 / 存活检测 / API 提取 / 线程本地绑定 |
| `src/input_tools.py` | **新** | 输入体检 + 本地查重 + 代理存活检测 |
| `src/wincompat.py` | **新** | Windows 版本兼容层（Win7~Win11 的 DPI/API 降级链） |
| `src/egress_guard.py` | **新** | 出口红线守卫（三项强制校验） |
| `src/netguard.py` | **新** | 网络守卫，强制所有出站走代理 |
| `src/servers.py` | **新** | 53 个区服的映射表 |
| `src/app_config.py` | **新** | 配置管理（路径全部相对化，无写死） |
| `src/build_exe.py` | **新** | PyInstaller 打包脚本（内嵌 OCR 引擎） |
| `src/make_icon.py` | **新** | 生成多尺寸图标 |
| `src/make_version.py` | **新** | 生成 Windows 版本资源 |
| `tools/pick_text_linux.py` | **新** | Linux 原生验证码求解器（研发中，见下） |
| `tools/proxy_check.py` | **新** | 代理批量体检工具 |
| `docs/*.md` | **新** | 15 份文档：使用说明、交付说明、架构、链路、部署报告等 |
| `samples/*.jpg` | **新** | 5 张验证码样本（用于自测） |

### Added

- **完整的批量扫号链路**：登录 → 查角色 → 输出表，格式与老板原软件逐字节一致
- **本地 OCR 集成**：自动拉起识别引擎并点「启动」，不再需要人工干预
- **Vanguard 机器人校验自动解锁**：命中校验时自动串行解锁后继续
- **代理池**：SOCKS5/HTTP 自动识别、并发体检、按延迟排序、换代理重试
- **代理 API 提取**：支持「粘贴网址一键拉取」，并按红线过滤国内出口
- **输入体检与查重**：加之前告诉你「共多少条 / 格式错几条 / 重复几条 / 历史跑过几条」
- **进度显示**：成功 / 失败 / 跳过 / 时速 / 预计剩余 + 失败原因分布
- **崩溃兜底**：未捕获异常统一记盘 + 弹窗，不会静默退出
- **Windows 兼容层**：启动探测系统版本，DPI 与 API 按能力降级
- **端到端自测模式**（`--e2e`）：真实驱动界面跑完整流程并断言结果

### Fixed

发布前踩到并修掉的坑（都是实测出来的）：

| 症状 | 根因 |
| :--- | :--- |
| Vanguard 恒报「会话已失效」 | **登录走代理池、查询偷偷走默认出口** —— 代理是线程本地变量，工作线程没带过去 |
| OCR 服务起不来 | 窗口一出现就找「启动」按钮，但**子控件还没创建完** |
| 文字发虚（"720p 感"） | 没声明 DPI 感知，Windows 把界面放大 2 倍 |
| 对话框按钮点不到 | 尺寸算错，保存按钮被挤出可见区 |
| 自检每次报 `main thread is not in main loop` | **工作线程里读 Tk 变量** |
| 进度条永远 0 | 日志搬运循环里单条失败会**永久杀死循环** |
| 日志一直"重试中" | 噪声占 57%（117 行轮次 + 190 行 vanguard vs 17 行结果） |
| 端到端报告写不出来 | `wm_gui.py` **忘了 `import io`** |
| 进程静默消失 | `--windowed` 下未捕获异常直接退出 |
| `app_config` 无限递归 | `load()` 调 `default_ocr_dir()`，后者又反过来调 `load()` |
| exe 尺寸算错一半 | Tk 的 geometry 与字体缩放规则不同，需要分开处理 |

### Verified

**端到端验收（真实驱动界面，6 个真账号）**：

```
[1] 界面已建立  字体=Microsoft YaHei UI/Consolas  DPI=192.0
[2] 已填表  账号=账号.txt(6个)  代理=代理.txt(100条)
[3] 点「环境自检」…    ✅ 三项全过
[4] 点「开始扫描」…    扫描完成  用时 30.6 分
[5] 断言结果
    结果行数: 6
    ✅ 全部 6 行格式正确
  端到端测试结论: 通过 ✅
崩溃日志: 无 ✅
```

**独立自检**：全部通过（识别端 / 出口红线 / 依赖 / OCR 实拍）

**产物**：`完美世界扫号工具.exe` 81.69 MB，版本 1.2.0.0，
内含 OCR 引擎（`OCR.dll` 52 MB + `XYLib.dll` 24 MB，占 93%）

**已验证环境**：Windows 11 (10.0.26100) x64 @ 200% 缩放

### Known Issues

| 问题 | 状态 |
| :--- | :--- |
| Windows 10 / 8.1 / 7 未真机验证 | 代码有完整降级链，但没真机跑过 |
| Linux 不支持 | OCR 引擎依赖 D3D11 + Media Foundation，Wine 跑不动（详见 `docs/服务器部署报告.md`） |
| Linux 原生 OCR 求解器 | `tools/pick_text_linux.py` 已能准确检测文字框、准确识别提示行；散布字符识别约 40%，模板匹配单字约 50%，**尚未达到可用精度** |
| 并行数上限 | 单 IP 最多 2；超过 8 会被风控 |

### Security

- 仓库内所有真实账号、代理凭据、服务器密码**已脱敏**
- 提供 `账号.txt.example` / `代理.txt.example` 模板
- `.gitignore` 已排除所有凭据文件与运行时数据
