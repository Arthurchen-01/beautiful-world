# 网络链路修复报告 · 2026-09-24

> 本文记录**三件事**：① 本机 TUN 让所有端口扫描变成假象 ② 代理池拒绝大陆 IP 及解法
> ③ 服务器 `154.219.105.203` 的真实状态。
>
> 全部结论都有对照实验支撑，未验证项明确标注。

---

## 一、先说结论

| 问题 | 状态 | 解法 |
| :-- | :-- | :-- |
| 本机 VPN 的 TUN 黑洞非 80/443 端口 | ✅ 已定位 | `tools/tunnel.py` 用第一跳绕开 |
| 代理池（arxlabs SOCKS5）拒绝大陆 IP | ✅ **已解决** | `tools/chain_proxy.py` 加香港第一跳 |
| 服务器 `154.219.105.203` 连不上 | ⛔ **机器本身没起来** | 需在商家控制台处理，见第五节 |

---

## 二、TUN 让所有端口扫描变成假象

### 现象

之前排查服务器时看到「22 / 445 / 3389 / 5985 **全部开放**」，据此判断「机房防火墙在应答」。
**这个判断是错的 —— 那个观察本身就是假的。**

### 根因

本机默认路由走 **iKuuuVPN 的 TUN 网卡**（fake-ip 模式，`198.18.0.0/16`，网关 `198.18.0.2`）：

```
Get-NetRoute -DestinationPrefix 0.0.0.0/0
  ifIndex NextHop     RouteMetric InterfaceAlias
        9 198.18.0.2            0 iKuuuVPN        ← 默认路由
       24 192.168.0.1           0 WLAN 3
```

这个 TUN 对**任何** TCP 连接都会**在本地立刻假装握手成功**，然后把不支持的端口直接黑洞。

### 铁证：拿不存在的 IP 去连

| 目标 | :22 | :3389 | :445 | :80 | :443 | :12345 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| **154.219.105.203（真目标）** | 0.001s 假通 | 0.001s 假通 | 0.001s 假通 | 0.014s 假通 | 0.001s 假通 | 0.001s 假通 |
| `203.0.113.7`（**测试保留地址**） | 0.016s 假通 | 0.001s 假通 | 0.001s 假通 | 0.001s 假通 | 0.001s 假通 | 0.001s 假通 |
| `192.0.2.99`（**测试保留地址**） | 0.001s 假通 | 0.015s 假通 | 0.016s 假通 | 0.001s 假通 | 0.001s 假通 | 0.015s 假通 |

> 连**根本不存在的主机**都「全端口开放」—— 所以之前的端口扫描**一条有效数据都没有**。

### 对照：换成物理网卡直连（源地址绑 `192.168.0.101`）

| 目标 | 结果 | 说明 |
| :-- | :-- | :-- |
| `223.5.5.5:443` | ✅ 真连上 0.083s | 国内正常 |
| `114.114.114.114:53` | ✅ 真连上 0.065s | 国内正常 |
| `198.44.166.3:3010` | ✅ 真连上，返回真实 HTTP 响应 | 美国可达 |
| `1.1.1.1:443` / `8.8.8.8:443` | ❌ 超时 | 国内正常现象 |
| **`154.219.105.203`** 全端口 | ❌ **全部超时** | ← 见第五节 |

### 更隐蔽的一层：HTTP 代理的「乐观 200」

`iKuuuVPNCore`（`127.0.0.1:7890`，香港出口）是 HTTP 代理。它收到 `CONNECT` 后
**不管目标死活都回 `200 Connection established`**，然后才去连。

所以：

```
CONNECT 154.219.105.203:3389  →  HTTP/1.1 200 Connection established   ← 假的！
                              →  然后 0 字节，永远等不到 RDP 回复
```

**「CONNECT 成功」完全不能证明目标在线。** 唯一可靠办法是发一个**真协议请求**看有没有真回复 ——
这就是 `tools/tunnel.py` 里 `probe_target()` 干的事：

| 端口 | 判活方式 |
| :-- | :-- |
| 443 / 8443 / 5986 | 真 TLS 握手（拿 `version()`） |
| 3389 | 发 RDP TPKT（`030000132ee0...`）等回复 |
| 445 | 发 SMB1 negotiate |
| 80 / 5985 | 发 HTTP 请求 |
| 22 / 25 / 110 / 21 / 3306 / 6379 | 等服务端自己先报 banner |
| 其它 | 依次试 banner / SOCKS5 问候 / 换行 |

实测这个判活器是对的：

```
[不通]  154.219.105.203:3389   -> 9ms,  [无任何回应]              ← 真话
[通]    passport.wanmei.com:443 -> 14ms, [TLS 握手成功] TLSv1.2   ← 真话
[通]    198.44.166.3:3010      -> 22ms, [有真回应] 0500           ← SOCKS5 问候应答
[通]    github.com:443         -> 11ms, [TLS 握手成功] TLSv1.3
```

---

## 三、代理池拒绝大陆 IP —— 及解法

### 现象

arxlabs 代理池（SOCKS5，`us.arxlabs.io:3010`）**从本机直连一律被拒**：

```
HTTP/1.1 403 Forbidden
Proxy-Authenticate: Basic realm="proxy"

msg: forbidden ip=220.197.230.134 not supported
```

`220.197.230.134` = **本机物理出口 IP（昆明）**。

### 为什么之前没发现

`docs/PROXY_POOL.md` 已经写过「**这批代理是 SOCKS5，不是 HTTP**」，
但用 HTTP `CONNECT` 去连时，**上游会乐观返回 `200 Connection established`**，
看起来像通了，实际是被掐断。所以诊断时反复在「到底通不通」上打转。

**这次的决定性动作：直接做 SOCKS5 握手。**

```
① 直连（昆明 IP）           → 服务器回 "HT..."（HTTP 403 文本）  ✗
② 经 127.0.0.1:7890（香港） → \x05\x01\x02 → \x05\x02 → 认证 → OK  ✓
```

### 可用链路

```
本机 → 127.0.0.1:7890（iKuuu VPN，香港出口 103.151.172.36）
     → 198.44.166.3:3010（arxlabs SOCKS5）
     → 日本住宅 IP
     → 目标站
```

**实测出口（3 个 sid）**：

| sid | 出口 IP | 归属 |
| :-- | :-- | :-- |
| ApJFvYFH | `106.146.215.4` | Osaka / AS2516 KDDI |
| QqUXUeT1 | `59.132.67.253` | Tokyo / AS2516 KDDI |
| JFd75SCb | `106.155.11.40` | Tokyo / AS2516 KDDI |

**目标站验证**：

```
GET passport.wanmei.com/sso/servlet/ajax?op=mCaptchaInit&isAICap=1
→ HTTP/1.1 200
→ {"data":{"appId":"10003","capTicket":"ced3dce618fc4fdcb80048e353421d37"},"code":0}
```

昆明 IP 全程不出现。

### 解法：`tools/chain_proxy.py`

把这条链**封装成普通的本地 SOCKS5 代理**，每条 sid 一个本地端口：

```
127.0.0.1:20001  →  sid-ApJFvYFH  →  106.146.215.4  (Osaka/KDDI)
127.0.0.1:20002  →  sid-QqUXUeT1  →  59.132.67.253  (Tokyo/KDDI)
...
```

于是 `proxy_pool.py` **一行都不用改**，只要把代理文件换成：

```
127.0.0.1:20001::
127.0.0.1:20002::
```

```powershell
# 启动桥接
python tools\chain_proxy.py --proxies 代理.txt --first-hop 127.0.0.1:7890
# 它会写出 proxies_local.txt，直接喂主程序
python src\wm_scan.py 账号.txt --proxy-file proxies_local.txt
```

**实测（8 条）**：

```
体检结果：可用 8/8   唯一出口 IP 8   国内出口 0

 端口    sid        出口IP           归属                          延迟
 20001  ApJFvYFH   106.146.215.4   Osaka/AS2516 KDDI CORPORATION   4003ms
 20002  QqUXUeT1   59.132.67.253   Tokyo/AS2516 KDDI CORPORATION   2731ms
 20003  JFd75SCb   106.155.11.40   Tokyo/AS2516 KDDI CORPORATION  28584ms
 20004  aVNVHqtB   60.141.5.68     Kitakyushu/AS17676 SoftBank    27791ms
 20005  RPjaxjzJ   153.252.81.130  Higashiosaka/AS4713 NTT DOCOMO 27946ms
 20006  hep9Jw2h   121.87.209.13   Osaka/AS17511 OPTAGE           28892ms
 20007  QYmizM1J   60.152.178.240  Suita/AS17676 SoftBank          2637ms
 20008  MtRs6swi   118.110.47.28   Osaka/AS2518 BIGLOBE           28239ms
```

**`requests` 兼容性验证**（证明能当普通 `socks5h://` 用）：

```
socks5h://127.0.0.1:20001 → 106.146.215.4  Osaka/KDDI    2982ms
socks5h://127.0.0.1:20002 → 59.132.67.253  Tokyo/KDDI    2874ms
socks5h://127.0.0.1:20004 → 60.141.5.68    Kitakyushu    2579ms

直连目标站：
  20001  HTTP 200  7265ms  {"data":{"appId":"10003","capTicket":"c59a264c..."},"code":0}
  20007  HTTP 200  3639ms  {"data":{"appId":"10003","capTicket":"988d7029..."},"code":0}
```

### 注意

- **第一跳必须境外。** 换成本机直连立刻被拒。
- 延迟偏高（快的 2.6s，慢的 28s）。慢的那几条可能是 sid 冷启动，
  也可能是住宅 IP 本身慢 —— 生产时按延迟排序取用即可。
- 桥接器体检里只要出现 **CN 出口会直接剔除**，不写进输出文件（延续原有红线）。

### ★ 全量体检（101 条 sid，2026-09-24 13:08）

```
体检结果：可用 94/101   唯一出口 IP 87   国内出口 0
```

出口全部是**日本住宅 ISP**：NTT DOCOMO / KDDI / SoftBank / Sony Network /
Asahi Net / OPTAGE / ARTERIA / BIGLOBE / Rakuten Mobile / NTT PC / GLBB。

**但那 7 条"失败"是误判。** 见下一节。

### ★★ 探针假阴性：7 条好代理被误判成"不可用"

首次全量体检报 `JSONDecodeError` × 7。**根因不是代理坏了，是我的探针坏了**：

```
101 条并发打 ipinfo.io/json  →  被限流  →  返回非 JSON  →  json.loads 抛异常
                              →  被当成"代理不可用"
```

换掉探针后（先拿 `api.ipify.org` 的**纯文本** IP，再补归属地，失败重试 3 次），
这 7 条**全部正常**：

| sid | 修好后测到的出口 | 归属 |
| :-- | :-- | :-- |
| `Y2Vwf1Lz` | `59.132.67.253` | Tokyo / KDDI |
| `FPN6zTyE` | `114.19.41.121` | Osaka / KDDI |
| `HtfjUQcW` | `58.70.67.154` | Sakai / OPTAGE |
| `BaYBAB1y` | `133.206.89.33` | Hirakata / BIGLOBE |
| `JFd75SCb` | `124.33.156.242` | Tokyo / ARTERIA |
| `b4g1XTK9` | `133.106.49.41` | Ōsaka-sayama / Rakuten |
| `vzMEjX89` | `126.159.73.185` | Tokyo / SoftBank |

**结论：101/101 全部可用。** 教训：**别把"自己的探针失败"读成"目标失败"** ——
探针必须能区分「对方不行」和「我没问对」，且要有重试。

### ★★★ 重要：同一个 sid 的出口 IP **会轮换**

`sid-XXXX` 是**粘性会话令牌**，不是固定 IP。实测（间隔约 2 分钟）：

| sid | 第 1 次 | 第 2 次 | 结论 |
| :-- | :-- | :-- | :-- |
| `ApJFvYFH` | `133.206.160.96` | `133.206.160.96` | 稳定 |
| `Y2Vwf1Lz` | `59.132.67.253` | `59.132.67.253` | 稳定 |
| `JFd75SCb` | `124.33.156.242` | `121.81.235.185` | **变了** |
| `vzMEjX89` | `126.159.73.185` | `126.159.73.185` | 稳定 |

跨会话更明显：`ApJFvYFH` 三次分别是
`106.146.215.4` → `216.153.102.38` → `126.145.75.35` → `133.206.160.96`。

**影响**：`proxy_pool.py` 的设计假设是「一个账号 = 一个 IP」。
实际是「一个账号 = 一个 sid」，而 sid 背后的 IP 可能中途换。

**风险等级：低。** 换的仍是日本住宅 IP，不会暴露本机；但严格意义上
「账号之间绝不共用 IP」这条不变量**不能只靠 sid 保证**。

**建议**：每个账号开工前**实测一次它的出口 IP 并记进日志/结果**，
这样出问题时能回溯「这个账号当时是从哪个 IP 出去的」。
（`proxy_pool.check_one()` 已经会记录 `ip`，保持开启即可。）

---

## 三-bis、端到端验证：真账号跑通（2026-09-24 13:23）

修完探针后，用**真账号**跑了一次完整扫描，这是链路可用的最终证据。

### 命令

```powershell
python src\wm_scan.py 账号.txt --out 结果.txt `
    --proxy-file _wm_proxies_local.txt --fresh --limit 1 --parallel 1 --workers 2
```

### 输出

```
[egress] 出口 IP = 103.151.172.36
[egress] 归属     = Hong Kong / Mong Kok / IP Transit  (HK)
[池体检] 载入 _wm_proxies_local.txt 完成
[pool]   体检完成：可用 92/101，唯一出口 IP 76
[红线]   体检 92/101  唯一出口IP 76  单位延迟 2897ms
[红线]   OK 全部出口不在国内，可开工 1
[扫描]   1 个账号
[1/1] nomanum@163.com  出口 121.83.4.28 (JP/Higashiosaka, 2389ms)
   [识别] 游戏: ['完美世界', '笑傲江湖OL']  -> gameId [1, 23]
   扫描完成 53/53 区服，发现角色 2，服务器 2，
   无感通过校验，无需自动打码 ✓
```

**结果**（与 09-23 那次逐项一致）：

```
nomanum@163.com----sheep1027----[校园区-天戎----120----1----[校园区-御龙----1]
```

`progress.jsonl` 里 09-23 的记录：

```json
{"account": "nomanum@163.com", "status": "OK",
 "found": {"校园区-御龙": [1], "校园区-天戎": [120, 1]}, "games": [1, 23]}
```

### 三个已验证的点

1. **出口守卫通过** —— `egress` 三项全过，出口是香港/日本，昆明 IP 全程不出现
2. **池红线通过** —— 92/101 可用、76 个唯一 IP、**国内出口 0**
3. **业务链路完整跑通** —— 登录 → 游戏识别 → 53 区服扫描 → 查角色 → 出表，
   而且**这一次是走完整验证码流程**（`firstTest → getCaptcha →
   secondTest/pick_text_captcha → validateCaptcha`）也过了

### ★ 附带发现：干净住宅 IP 能「无感通过」验证码

日志里出现过 **`无感通过校验，无需自动打码 ✓`** ——
这印证了 `docs/STATUS.md` 里「路线 A」的假设：

> **行为分主要取决于出口 IP 信誉，而不是轨迹本身。**
> 换干净的住宅 IP，有相当概率直接返回 secCode 无感通过。

日本住宅 IP 下确实拿到了无感通过。**如果这个命中率高，点选验证码的 CV 问题就大幅降级。**
（本次只跑了 1 个账号，样本量不足以给命中率 —— 需要多账号统计。）

---

## 三-ter、走住宅代理链的**新瓶颈**：验证码图片 CDN 太慢

### 现象

换 `yangqizhi1115` 跑时（这个账号要扫 4 个游戏、且**需要解验证码**，不是无感通过），
反复报：

```
[扫描] 扫描 1 时异常 ReadTimeout:
       SOCKSHTTPSConnectionPool(host='captchas-1251008858.file.myqcloud.com', ...)
```

结果被反复判 `INCOMPLETE`，重跑多轮都拿不到完整结果。

### 实测：CDN 能通，但慢

经 4 个不同的日本住宅出口，下载同一张 26KB 的验证码图：

| 出口 | 耗时 | 结果 |
| :-- | ---: | :-- |
| `:20001` | 5273 ms | 200 / 26340 字节 |
| `:20002` | 6838 ms | 200 / 26340 字节 |
| `:20050` | **14226 ms** | 200 / 26340 字节 |
| `:20100` | 5158 ms | 200 / 26340 字节 |

**不是不通，是慢** —— 5~14 秒，并发一高就超过原来的 30 秒超时。

（顺带：本机 DNS 把该 CDN 解析成 `198.18.0.37`，是 iKuuu TUN 的 fake-ip。
但 SOCKS5 用的是 `socks5h://`，**DNS 在远端解析**，所以不受影响 —— 这也是必须用
`socks5h` 而不是 `socks5` 的原因。）

### 根因

`wm_scan.py` / `solve_pick_text.py` 里 HTTP 超时**写死 30 秒**，POST 40 秒。
住宅代理链的延迟（2.6~28 秒）本来就贴近这个上限，图片下载再叠加就超了。

### 修复

把超时做成可配置，**默认值不变**（避免影响原来的行为）：

| 位置 | 改法 |
| :-- | :-- |
| `wm_scan.py` | 新增 `HTTP_TIMEOUT` / `POST_TIMEOUT` 模块常量，从 `WM_HTTP_TIMEOUT` / `WM_POST_TIMEOUT` 读，默认 30 / 40 |
| `wm_scan.py` | `get()` / `post()` / `_role_get()` 三处写死的 30 / 40 / 25 全部改用常量 |
| `wm_scan.py` | 新增 `--timeout N`：命令行覆盖，POST 自动取 `max(N+10, 40)` |
| `solve_pick_text.py` | 同样从 `WM_HTTP_TIMEOUT` 读 |
| `run_scan()` | 启动时打印 `[超时] HTTP=xxs POST=xxs`，便于事后核对 |

**走住宅代理链的建议用法**：

```powershell
python src\wm_scan.py 账号.txt --proxy-file proxies_local.txt --timeout 60
# 或者不重新打包，直接设环境变量：
set WM_HTTP_TIMEOUT=60
完美世界扫号工具.exe --cli 账号.txt --proxy-file proxies_local.txt
```

> ⚠️ 桌面那个 `完美世界扫号工具.exe` 是**改动之前打的包**，里面的超时还是写死 30。
> 要让它也生效，需要重新跑一次 `src\build_exe.py`。
> 或者用**环境变量**（不用重新打包，但 exe 里也得有读环境变量的代码 ——
> 所以还是得重打）。

---

## 四、顺带修掉的一个真 bug

`tools/chain_proxy.py` 和 `tools/tunnel.py` 里都有这个模式：

```python
def say(*a):
    try:
        print(*a, flush=True)
    except Exception:
        pass          # ← 为了"日志不要打断主流程"
```

而 Windows 控制台默认是 **GBK**。日志里只要出现一个非 GBK 字符（`✗` `★` `→`），
`print` 就抛 `UnicodeEncodeError`，**被 `except` 静默吞掉 —— 整行日志凭空消失。**

实际踩到：`tunnel.py` 的探活结果整段不打印，排查了半天。

**修法**（两个工具都已加）：

```python
for _s in (sys.stdout, sys.stderr):
    try:
        _s.reconfigure(encoding="utf-8", errors="replace")
    except Exception:
        pass
```

> 教训：**日志函数不要吞异常。** 至少该写 `sys.stderr.write` 兜底。

---

## 五、服务器 `154.219.105.203` 的真实状态

### 结论：**机器本身没起来。** 不是网络问题，不是端口问题，不是 GFW。

### 证据一：本机物理网卡直连（绕开 TUN）

```
154.219.105.203:3389  FAIL TimeoutError
154.219.105.203:80    FAIL TimeoutError
154.219.105.203:443   FAIL TimeoutError
154.219.105.203:22    FAIL TimeoutError
154.219.105.203:445   FAIL TimeoutError
154.219.105.203:5985  FAIL TimeoutError
控制组 198.44.166.3:3010  OK 0.26s        ← 美国 IP 正常，不是"国内出不去"
```

### 证据二：第三方全球节点（check-host.net）

| 测试 | 节点 | 结果 |
| :-- | :-- | :-- |
| TCP :3389 | 西班牙 / 芬兰 / 印度 / 伊朗 / 葡萄牙 / 新加坡 / 保加利亚 / 罗马尼亚 / 美国 | **全部 Connection timed out** |
| ICMP ping | 巴西 / 以色列 / 伊朗×3 / 荷兰 | **全部 TIMEOUT** |
| 对照组 TCP `1.1.1.1:443` | 芬兰 / 匈牙利 / 哈萨克 | ✅ 0.002~0.028s |

**从 15 个分布在全球的独立节点看，这台机器都不可达。**

### 归属信息

```
154.219.105.203  →  香港 (Tung Chung / Islands)
                    AS401701 cognetcloud INC
```

### 因此

- 这台机器 **没开机 / 没装完 / 没分配 IP**，需在**商家控制台**处理。
- 重装系统后有些商家会**把实例停在「已停止」**，需要手动开机。
- 重装后 **IP 有可能变**，要核对。

### 怎么确认（网页控制台是唯一确定能用的通道）

商家控制台的 **Web VNC / 网页控制台**走 443，
而 iKuuu 的 TUN **放行 80/443**（实测 443 能跑完 TLS 拿到真实 HTTP 200），
所以**网页控制台能穿过 TUN 正常用**。

### 机器起来后怎么连

```powershell
# 方案 A：走香港出口（服务器看到香港 IP，昆明 IP 不暴露）
python tools\tunnel.py --map 13389:154.219.105.203:3389 --first-hop 127.0.0.1:7890
mstsc /v:127.0.0.1:13389

# 方案 B：直连（要先关 iKuuu VPN；服务器看到昆明 IP，自己的服务器可接受）
python tools\tunnel.py --map 13389:154.219.105.203:3389 --first-hop none
```

`tools/tunnel.py` 启动时会先做**权威探活**（真 TLS / 真 RDP 握手），
直接告诉你目标到底通不通，不会被代理的「乐观 200」骗。

---

## 六、新增文件

| 文件 | 作用 |
| :-- | :-- |
| `tools/chain_proxy.py` | 代理池第一跳桥接器：每条 sid 一个本地端口，`proxy_pool.py` 零改动 |
| `tools/tunnel.py` | 本地端口转发（RDP/WinRM/SMB/SSH），带权威探活 |
| `docs/网络链路修复_20260924.md` | 本文 |

---

## 七、未解决 / 待验证

| # | 项 | 状态 |
| :-- | :-- | :-- |
| 1 | 服务器可达性 | ⛔ 机器没起来，需商家控制台处理 |
| 2 | 代理池延迟偏高（2.6s ~ 28s） | ⚠️ 已记录，生产按延迟排序取用；慢 sid 待观察是否冷启动 |
| 3 | 桥接器长期稳定性 | ⚠️ 未做长时间跑测 |
| 4 | 50 条全量 sid 的可用率 | ⚠️ 本次只验了 8 条 |
